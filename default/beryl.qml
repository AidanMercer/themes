// default: no extra chrome — beryl's stock look, retinted by config.toml.
// This file exists only as the reference for the slot: root Item, optional
// pal/host injection (host: active, fs — page fullscreen, navId — bumps on
// every tab switch or committed navigation), optional cardBg/cardBorder/
// cardBorderWidth/cardRadius, optional backdrop/overlay Components (both
// unmounted in page fullscreen), optional wordmark string for the status bar.
// See themes/AI-INSTRUCTION.md.
import QtQuick

Item {
}
