// default: no extra chrome — cobalt's stock look, retinted by config.toml.
// This file exists only as the reference for the slot: root Item, optional
// pal/host injection (host: active, navId — bumps on every Teams SPA
// navigation like a rail switch, loading), optional backdrop/overlay
// Components (backdrop sits under the glass bars AND the stripped transparent
// Teams regions; overlay rides above, below cobalt's modals), optional
// wordmark string for the status line.
// See themes/AI-INSTRUCTION.md.
import QtQuick

Item {
}
