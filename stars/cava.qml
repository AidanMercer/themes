import QtQuick

// stars: no visualizer. The vending-grid EQ was retired — this stub keeps the
// slot occupied so the shell's default Arch-logo cava doesn't mount instead.
Item {
    required property var pal
    anchors.fill: parent
}
