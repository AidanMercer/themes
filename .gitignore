# local AI prompt, not part of the themes themselves
AI-INSTRUCTION.md

# generated on theme apply — the switcher/videowall re-extract these from the
# wallpaper, no point versioning them
still.png
preview.png
*.still.png

# over github's 100MB hard limit, has to stay local (176M)
moon/wallpaper2.mp4

# stamped by theme-install.sh into marketplace-installed themes (the catalog
# commit at install time) — a local install artifact, not theme content
.mkt-version
